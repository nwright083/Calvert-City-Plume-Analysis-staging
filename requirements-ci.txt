# Python deps for the nightly GitHub Actions run (Linux). Loose pins — CI uses recent compatible builds.
# GRIB decode (Herbie → cfgrib → eccodes) also needs the system 'libeccodes-tools' apt package.
herbie-data>=2024.1
xarray>=2024.1
cfgrib>=0.9.10
eccodes>=1.6
pandas>=2.0
numpy>=1.26
openpyxl>=3.1
requests>=2.31
